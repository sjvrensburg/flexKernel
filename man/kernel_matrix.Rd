% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernels.R
\name{kernel_matrix}
\alias{kernel_matrix}
\title{Create a kernel matrix from data points}
\usage{
kernel_matrix(X1, X2 = NULL, kernel)
}
\arguments{
\item{X1}{First set of data points (each row is a data point)}

\item{X2}{Second set of data points (optional, defaults to X1)}

\item{kernel}{Kernel specification}
}
\value{
Kernel matrix K
}
\description{
This function computes the kernel matrix for a given set of data points
using a specified kernel.
}
\examples{
# Generate some data
X <- matrix(rnorm(20), 10, 2)

# Create a Gaussian kernel
kernel <- gaussian_kernel(1.0)

# Compute the kernel matrix
K <- kernel_matrix(X, kernel = kernel)
}
