% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernels.R
\name{Ops.kernel_spec}
\alias{Ops.kernel_spec}
\title{Overloaded operators for kernel specifications}
\usage{
\method{Ops}{kernel_spec}(e1, e2)
}
\arguments{
\item{e1}{First operand}

\item{e2}{Second operand (may be missing for unary operators)}
}
\value{
A new kernel specification
}
\description{
This function allows the use of arithmetic operators with kernel specifications,
providing an intuitive way to construct complex kernels.
}
\details{
Supported operations:
\itemize{
\item \code{kernel + kernel}: Creates a sum of kernels
\item \code{kernel * kernel}: Creates a product of kernels
\item \code{number * kernel} or \code{kernel * number}: Creates a scaled kernel
}
}
\examples{
# Create some basic kernels
g <- gaussian_kernel(1.0)
s <- sinc_kernel(0.5)

# Create complex kernels using operators
sum_kernel <- g + s          # Sum of kernels
product_kernel <- g * s      # Product of kernels
scaled_kernel <- 2.5 * g     # Scaled kernel

# Create a more complex kernel
complex_kernel <- 0.5 * g + 2.0 * (s * g)
}
