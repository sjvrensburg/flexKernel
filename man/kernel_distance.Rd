% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernels.R
\name{kernel_distance}
\alias{kernel_distance}
\title{Kernel distance between two sets of points}
\usage{
kernel_distance(X1, X2 = NULL, kernel)
}
\arguments{
\item{X1}{First set of data points (each row is a data point)}

\item{X2}{Second set of data points (optional, defaults to X1)}

\item{kernel}{Kernel specification}
}
\value{
Matrix of kernel distances
}
\description{
This function computes the pairwise kernel distances between two sets
of points. The kernel distance is defined as:
\deqn{d_k(x, y) = \sqrt{k(x, x) + k(y, y) - 2k(x, y)}}
}
\examples{
# Generate some data
X <- matrix(rnorm(20), 10, 2)

# Create a Gaussian kernel
kernel <- gaussian_kernel(1.0)

# Compute kernel distances
D <- kernel_distance(X, kernel = kernel)
}
